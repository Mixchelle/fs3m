DEBUG=true
SECRET_KEY=dev-secret
ALLOWED_HOSTS=["*"]

# escolha 1: SQLite (dev)
DB_URL=sqlite:///./db.sqlite3

# escolha 2: Postgres
# DB_URL=postgres://fs3m_user:fs3m_pass@localhost:5432/fs3m

# chaves de criptografia (gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEYS=YOUR_FERNET_KEY_1,YOUR_FERNET_KEY_0

# Em produção:
# DEBUG=false
# ALLOWED_HOSTS=["api.seu-dominio.com"]
# CORS_ALLOWED_ORIGINS=["https://app.seu-dominio.com"]
# CSRF_TRUSTED_ORIGINS=["https://app.seu-dominio.com"]
